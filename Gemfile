source "https://rubygems.org"

# Use the github-pages gem to specify the dependencies
# and versions that GitHub Pages uses.
gem "github-pages", group: :jekyll_plugins

# Explicitly list Jekyll, although 'github-pages' includes it, this can
# help when running locally.
gem "jekyll"
